# Infrastructure Utilities (`src/infra/`)

This directory contains system-level infrastructure utilities and helper scripts.

## Contents

- **`ChicagoChuck/`**: The name of remote Ollama server.

## Purpose

The `infra/` directory is intended for code and configurations that support the running of the application but are not part of the core agent or business logic.
